# 术语与边界

这份术语表只解决一件事：避免把增长设想和技术载体混在一起。

## Capability-Led Growth（CLG）

把一部分产品或专业服务能力送入用户已有环境，让它先完成真实任务，再观察这次使用是否
带来复用、注册、留资、销售机会或付费。

CLG 描述 PLG 在可分发能力出现之后的一条延伸路径，不是一种技术格式。

它延续 PLG 的核心逻辑：由使用驱动获客、转化、留存和扩张。区别在于，承担增长任务的
不再只能是完整产品，也可以是进入用户环境的能力单元。

## Capability Unit（能力单元）

围绕明确任务形成、可以被单独设计和评估的产品边界。

它可以依赖模型、运行环境、工具和数据，不必独立运行；也不必追求“最小”。关键在于团队
能否单独说清它的用户、任务、依赖、调用方式、质量、成本、风险和责任人。

## CLG 设计

围绕一个能力单元设计分发、来源归属、后续承接、反馈和测量机制。CLG 设计描述团队准备
如何让能力成为营销与增长触点。

## CLG 实践

把一项 CLG 设计放进真实场景，观察用户如何发现、使用、分享和继续关系，并根据结果修改
设计。成功与失败的实践都用于发展模型。

## CLG 模型

从多次 CLG 实践中逐渐形成的解释和设计框架。它用于回答企业应该选择什么能力、如何分发、
如何建立信任与承接，以及怎样判断这条路径产生了新的关系。

## 能力单元生命周期

一个能力单元从选择、设计、评测、包装、发布、分发、激活、归因、承接、迭代到退役的完整
过程。它把产品管理、增长、用户运营、治理和合规连接到同一个运营对象上。

## 能力关系

用户通过一次能力使用与提供方形成的关系。它可能发生在第三方环境中，也可能早于注册和
账号创建。能力关系是否能够被识别、延续和转化，是 CLG 用户运营的核心问题。

## 有效价值任务

能力成功完成了用户原本要解决的任务，并出现至少一个结果采用信号，例如保存、采用、分享、
继续任务或合理周期内复用。它比调用量更接近用户真实获得的价值。

## 能力合格关系（CQR）

Capability-Qualified Relationship。用户或组织已经通过能力获得真实价值，同时表现出匹配度、
持续任务需求或明确后续意图，并同意建立进一步关系。它是 CLG 对 PQL 思路的前置延伸。

## 增量

与没有发生这次能力分发的情况相比，新增的有效价值、关系或商业结果。归因说明某个结果与
能力有关，增量实验进一步判断结果是否由能力带来。

## 能力再分发系数

由分享、推荐或嵌入带来的新有效价值任务，除以产生过分享、推荐或嵌入的有效价值任务。它
衡量一次能力使用能否带来下一次有效使用，而不是只衡量分享动作。

## 可经营能力单元

在能力单元基础上，能够连接发现、执行、身份与同意、状态、计量、关系承接、商业化和生命
周期管理的一项能力。它不要求单个 Skill 自己实现全部功能，而要求能力能够连接到这些系统。

## 外部产品面

产品进入用户既有 Agent、CLI、工作流或第三方环境后的可使用界面。它通常由 Skill、执行
适配器、API、身份、状态、计量和商业接口共同组成，而不是一个独立文件或单一协议。

## 上下文内商业化

在用户已经获得价值、表达更深需求或遇到真实能力约束的任务上下文中，提供服务升级、套餐、
订单或支付路径。它强调商业动作与当前目标连续，并要求用户主动确认。

## Capability Pack（能力包）

围绕一组相关任务组织的能力组合，通常包含入口、路由、共享依赖和子能力。很多复杂结果
来自能力之间的组合，而不是某一个“最小单元”。

## Capability Slicing（能力切片）

从现有产品或服务中选择适合单独分发的任务边界。能力切片需要同时决定交付什么、保留
什么，以及这次结果如何自然连接到完整产品或服务。

## 营销 Hook

这里的 Hook 不是在结果里插入广告，而是围绕能力使用设计的产品机制：

- 说明能力来自谁、采用什么方法、谁对结果负责；
- 根据当前结果连接下一步产品或服务；
- 降低发现、安装、分享和再次调用的难度；
- 记录质量、复用、注册、留资、销售机会和付费。

## Skill、MCP 与 Agent

三者可能出现在同一条 CLG 路径里，但解决的问题不同：

| 对象 | 主要作用 |
|---|---|
| Agent Skill | 封装可加载的指令、流程和参考资料 |
| MCP Tool / Server | 为 Agent 提供工具、资源和上下文调用 |
| A2A AgentSkill | 在 Agent Card 中描述 Agent 能做什么 |
| Agent | 组织模型、指令、工具和运行行为来完成任务 |
| Registry | 帮助用户发现某一类 Skill、Server 或 Agent |

这些对象能够承载能力，不等于它们本身就是能力单元。

## 可分发、可移植与裂变

- **可分发**：能够被复制、安装、分享或调用。
- **可移植**：经过适配后，能够在多个声明支持的运行环境中维持可接受表现。
- **裂变**：一次分享持续带来新的有效使用。

可分发是条件，裂变是结果。没有传播数据以前，不应把两者混为一谈。

## 与相邻增长方式的关系

CLG 是 PLG 的一种分布式延伸，而不是与 PLG 并列的新大类。它与内容营销的区别在于能力会
执行任务；与普通免费工具的区别，则在于能力可以进入用户已有环境，并从这种分发中继续
建立关系。
